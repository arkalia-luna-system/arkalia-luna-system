name: Update Metrics

# Cron OFF — vitrine README manuelle.
on:
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Checkout arkalia-metrics-collector
        uses: actions/checkout@v5
        with:
          repository: arkalia-luna-system/arkalia-metrics-collector
          path: arkalia-metrics-collector
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run metrics collector
        run: |
          cd arkalia-metrics-collector
          python -m pip install --upgrade pip
          pip install -r requirements.txt || true
          # Exécute l'agrégation si le script existe
          python -m arkalia_metrics aggregate projects.json || echo "Metrics collection skipped"
        continue-on-error: true

      - name: Commit changes
        if: github.event_name != 'pull_request'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add metrics/ 2>/dev/null || true
          git diff --staged --quiet || git commit -m "📊 Auto-update metrics [skip ci]"
          git push || echo "No changes to commit"
