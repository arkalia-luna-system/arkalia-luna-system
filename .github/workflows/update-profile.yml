name: 🌙 Mise à jour automatique du profil

on:
  schedule:
    # Exécute tous les dimanches à 2h du matin UTC
    - cron: "0 2 * * 0"
  workflow_dispatch: # Permet de déclencher manuellement

jobs:
  update-profile:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install dependencies
        run: |
          pip install --upgrade pip
          pip install requests

      - name: 🔍 Découvrir les projets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python update-profile.py --export config/projects-data.json

      - name: 📝 Mettre à jour le README
        run: |
          python auto-update-readme.py

      - name: 📤 Commit et push
        run: |
          git config --local user.email "arkalia.luna.system@gmail.com"
          git config --local user.name "GitHub Action"
          git add config/projects-data.json README.md
          git diff --staged --quiet || git commit -m "🌙 Mise à jour automatique : $(date +'%Y-%m-%d')"
          git push
